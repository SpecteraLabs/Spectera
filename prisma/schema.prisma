datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model GuildSettings {
  id  String  @unique
  prefix  String  @default("s!")
  autorole  String?
  loggingChannel  String?
  muterole  String?
  modRoles  String[]
}