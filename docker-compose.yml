version: "3.9"

services:
  spectera-bot:
    build:
      context: .
      target: development
    env_file: .env
    volumes: 
      - ./:/opt/app
  postgres:
    container_name: postgres
    image: postgres:14-alpine
    expose:
      - "5432"